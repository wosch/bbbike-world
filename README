Required perl modules:
------------------------------------

see ../bin/bbbike-build-runtime-perl.pl

Required debian modules
------------------------------------

sudo apt-get install \
  pbzip2 bzip2 git-core screen lynx curl wget time \
  lsof whois pigz make acct telnet dnsutils bc bsd-mailx \
  buffer pktstat emacs unzip vim \
  python openjdk-6-jdk \
  libhtml-tagcloud-perl libtext-unidecode-perl munin \
  perltidy libxml-atom-simplefeed-perl libjson-perl liburi-perl \
  libxml-libxml-perl libyaml-perl libyaml-syck-perl tidy libxml-simple-perl \
  libdevel-size-perl libtie-ixhash-perl libdevel-size-perl \
  libbsd-resource-perl libobject-realize-later-perl libemail-valid-perl

# daemons
sudo apt-get install lighttpd

# reverse proxy
sudo apt-get install varnish 

# tile server
apt-get install subversion


# osmosis from repo
sudo apt-get install osmosis

sudo cpan GIS::Distance::Lite

Required MacPorts modules
------------------------------------
sudo port install pbzip2 bzip2 git-core lynx curl wget whois pigz bc buffer \
	lighttpd munin tidy varnish openjdk6 \
	p5-bsd-resource p5-uri p5-json p5-xml-simple p5-tie-ixhash \
	p5-yaml-syck p5-yaml p5-perl-tidy p5-libwww-perl

sudo perl -MCPAN -e 'install BSD::Resource'
sudo perl -MCPAN -e 'install Text::Unidecode'
sudo perl -MCPAN -e 'install HTML::TagCloud'
sudo perl -MCPAN -e 'install XML::Atom::SimpleFeed'
sudo perl -MCPAN -e 'install XML::LibXML::Reader'
sudo perl -MCPAN -e 'install Object::Realize::Later'
sudo perl -MCPAN -e 'install Email::Valid'


Development
------------------------------------
# fetch & merge from main branch
git fetch eserte
git merge eserte/master

# download planet.osm, extract and convert city data
$ make update-database

# extract data for a city and build a bbbike database
$ make CITIES="Augsburg Halle" cities convert

# fetch data for a city and build a bbbike database
$ make CITIES="Augsburg Halle" fetch convert

# extract and convert only
$ make bbbike_download="" update-database

# convert only
$ make bbbike_download="" bbbike_extract="" update-database

# check a city area
$ ./world/bin/bbbike-db --city-stat | grep Bielefeld
Bielefeld =>    69.3    Bielefeld:de,en,da,es,fr,hr,nl,pl,pt,ru,zh 

