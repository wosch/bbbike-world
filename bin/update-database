#!/bin/sh
#
# wrapper for Makefile bug
#

set -e

tmp=tmp
mkdir -p $tmp

: ${bbbike_cleanup="distclean planet-download"}

for i in $bbbike_cleanup cities-parallel convert-wgs84 convert-bbbike
do
	\time make -s $i > tmp/log.$i 2>&1
done


