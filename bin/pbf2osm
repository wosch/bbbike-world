#!/bin/sh
#
# pbf2osm - convert PBF format to OSM XML

file=$1

if ! [ -f "$file" ]; then
	echo "usage file"
	exit 1
fi

# run in quiet mode
global_opt=-q

osmosis $global_opt--read-pbf "$file" --write-xml /dev/stdout 

